#!/usr/bin/env node
var argv = require('minimist')(process.argv.slice(2), {boolean: true});
var tile = require('../index.js').tile;

tile(argv._[0], argv._[1], argv._[2], {
    tileSize: argv.tileSize,
    tmpDir: argv.tmpDir,
    zeroZoomOut: argv.zeroZoomOut
});